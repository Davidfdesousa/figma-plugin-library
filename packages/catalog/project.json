{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "@plugin-factory/catalog",
  "targets": {
    "generate": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsx src/generate-catalog.ts",
        "cwd": "{projectRoot}"
      },
      "outputs": ["{projectRoot}/dist"],
      "cache": false
    }
  }
}
